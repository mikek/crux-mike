# Description: A free chess engine
# URL: http://www.gnu.org/software/chess/chess.html
# Maintainer: Mikhail Kolesnik, mike at openbunker dot org
# Depends on:

name=gnuchess
version=5.07
release=2
source=(http://ftp.gnu.org/pub/gnu/chess/$name-$version.tar.gz
		gnuchess-gcc4.patch)

build(){
	cd $name-$version
	patch -p1 -i ../gnuchess-gcc4.patch
	sed -i "s/getline/gg_getline/g"  src/common.h
	sed -i "s/getline/gg_getline/g"  src/input.c
	./configure --prefix=/usr
	make
	make DESTDIR=$PKG install
}
