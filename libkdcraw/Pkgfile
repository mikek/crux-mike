# Description: RAW image handling library for KDE
# URL: https://sourceforge.net/project/showfiles.php?group_id=149779&amp;package_id=230894
# Maintainer: Mikhail Kolesnik, mike at openbunker dot org
# Packager: Diederick de Vries, diederick at diederickdevries dot net
# Depends on: lcms

name=libkdcraw
version=0.1.9
release=1
source=(http://dl.sourceforge.net/sourceforge/kipi/$name-$version.tar.bz2)

build() {
	cd $name-$version
	./configure --prefix=/usr --disable-nls
	make
	make DESTDIR=$PKG install
	rm -rf $PKG/usr/share/locale
}
