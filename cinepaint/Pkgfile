# Description: Software tools for deep paint manipulation and image processing
# URL: http://www.cinepaint.org/
# Maintainer: Mikhail Kolesnik, mike at openbunker dot org
# Packager: Hannes Mayer, kontakt at ecarux dot de
# Depends on: gtk, python, openexr, fltk

name=cinepaint
version=0.22-1
release=2
source=(http://dl.sourceforge.net/sourceforge/$name/$name-$version.tar.gz)

build() {
	cd $name-$version
	./configure --prefix=/usr \
				--enable-gtk2 --enable-pygimp \
    			--with-python=/usr/bin/python2.5
	make
	make DESTDIR=$PKG install
	rm -rf $PKG/usr/share/locale
	find $PKG/usr -iname "*readme*" -delete
}
